<odoo>
    <!-- 1. Wizard Form View -->
    <record id="view_sales_product_summary_wizard_form" model="ir.ui.view">
        <field name="name">sales.product.summary.wizard.form</field>
        <field name="model">sales.product.summary.wizard</field>
        <field name="arch" type="xml">
            <form string="Sales Product Summary">
                <group>
                    <group>
                        <field name="date_from"/>
                    </group>
                    <group>
                        <field name="date_to"/>
                    </group>
                </group>
                <footer>
                    <button name="action_print_report" string="Print PDF" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- 2. Wizard Action -->
    <record id="action_sales_product_summary_wizard" model="ir.actions.act_window">
        <field name="name">Sales Product Summary</field>
        <field name="res_model">sales.product.summary.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- 3. Menu Item (Sales -> Reporting) -->
    <menuitem id="menu_sales_product_summary_report"
              name="Sales Product Summary"
              parent="sale.menu_sale_report"
              action="action_sales_product_summary_wizard"
              sequence="10"/>
</odoo>